# 🛡️ خطة التحسين الأمني الشاملة - مشروع "نبض" (Nabd Security Roadmap)

## 0. ملخص تنفيذي (Executive Summary)
يهدف هذا المستند إلى تحويل مشروع "نبض" من تطبيق في مرحلة النموذج الأولي (PoC) إلى منصة مؤسساتية آمنة (Enterprise-Grade Security). التركيز الأساسي هو حماية بيانات المستخدمين، منع إساءة استخدام موارد الذكاء الاصطناعي، وضمان استمرارية الخدمة.

**الحالة الحالية:** 🟠 متوسطة الخطورة (بعد الإصلاحات الأولية).
**الحالة المستهدفة:** 🟢 آمنة ومطابقة للمعايير الدولية.

---

## 1. البنية الأمنية (Security Architecture)

### الوضع الحالي
- تطبيق Next.js متصل مباشرة بـ Groq.
- تطبيق Python يعمل كخادم واحد (Monolith) مع أدوات خطرة (Code Execution).
- لا يوجد عزل حقيقي بين طبقات التطبيق.

### التحسينات المقترحة
1.  **Microservices Architecture:**
    - فصل **Frontend (SaaS)** عن **AI Core (Agent)**.
    - وضع خدمة الـ Agent داخل شبكة خاصة (Private Subnet) لا يمكن الوصول إليها إلا عبر API Gateway.

2.  **Sandboxing (العزل):**
    - **Code Interpreter Sandbox:** استبدال `python_repl` الحالي بحاويات مؤقتة (Ephemeral Containers) باستخدام تقنية مثل **gVisor** أو خدمة **E2B**. هذا يمنع أي كود خبيث من الوصول لملفات الخادم الرئيسية.
    - **Network Isolation:** منع حاويات الـ Agent من الاتصال بالإنترنت العام إلا عبر قائمة بيضاء (Allowlist) صارمة.

3.  **API Gateway:**
    - استخدام **Kong** أو **Traefik** كبوابة دخول موحدة.
    - وظيفتها: معالجة Rate Limiting، إنهاء SSL (TLS Termination)، والتحقق من التوكنات (JWT Validation) قبل وصول الطلب للخادم.

---

## 2. المصادقة والتفويض (Authentication & Authorization)

### الوضع الحالي
- واجهة وهمية (Fake Auth).
- مفاتيح API ثابتة (تم تحسينها جزئياً).

### التحسينات المقترحة
1.  **Identity Provider (IdP):**
    - استخدام **NextAuth.js (v5)** للمصادقة.
    - دعم **OAuth 2.0** (Google, GitHub) لتقليل مخاطر تخزين كلمات المرور.
    - تخزين الجلسات في قاعدة بيانات آمنة (Redis/Postgres) بدلاً من JWT فقط (لإمكانية إبطال الجلسة).

2.  **RBAC (Role-Based Access Control):**
    - تحديد أدوار: `User` (استخدام عادي)، `Subscriber` (موديلات متقدمة)، `Admin` (لوحة تحكم).
    - تطبيق سياسات الوصول (Policies) على مستوى الـ Middleware والـ API Routes.

3.  **MFA (Multi-Factor Authentication):**
    - تفعيل المصادقة الثنائية للمشرفين (Admins) باستخدام TOTP (تطبيقات Authenticator).

---

## 3. إدارة البيانات (Data Management)

### التحسينات المقترحة
1.  **التشفير (Encryption):**
    - **At Rest:** تشفير قاعدة البيانات (PostgreSQL TDE). تشفير الحقول الحساسة (مثل مفاتيح API الخاصة بالمستخدمين) باستخدام **AES-256-GCM**.
    - **In Transit:** فرض TLS 1.3 حصراً.

2.  **إدارة الأسرار (Secrets Management):**
    - الانتقال من ملفات `.env` إلى حلول مؤسساتية مثل **HashiCorp Vault** أو **AWS Secrets Manager** في البيئة الإنتاجية.

3.  **Data Isolation:**
    - في حال كان النظام Multitenant (تعدد مستأجرين)، يجب ضمان استخدام `tenant_id` في كل استعلام لقاعدة البيانات لمنع تسرب البيانات بين المستخدمين.

---

## 4. المراقبة والتسجيل (Monitoring & Logging)

### التحسينات المقترحة
1.  **Centralized Logging:**
    - استخدام مكدس **ELK (Elasticsearch, Logstash, Kibana)** أو **Datadog**.
    - تجميع سجلات: الوصول (Access Logs)، الأخطاء (Error Logs)، والأمان (Security Audit Logs).

2.  **Security Events (SIEM):**
    - تفعيل تنبيهات فورية عند:
        - فشل تسجيل الدخول المتكرر (Brute Force).
        - محاولات حقن (SQLi/XSS) المكتشفة.
        - استخدام غير طبيعي لموارد الـ API (Spike in usage).

3.  **Audit Trails:**
    - تسجيل كل إجراء "تغيير حالة" (Create, Update, Delete) مع: `من قام بالفعل؟`, `متى؟`, `ماذا تغير؟`, `من أي IP؟`.

---

## 5. الاختبار الأمني (Security Testing)

### التحسينات المقترحة
1.  **SAST (Static Application Security Testing):**
    - دمج أدوات مثل **SonarQube** أو **CodeQL** في GitHub Actions لفحص الكود تلقائياً عند كل Pull Request.

2.  **DAST (Dynamic Application Security Testing):**
    - تشغيل فحص دوري باستخدام **OWASP ZAP** على الـ Staging Environment.

3.  **Dependency Scanning:**
    - تفعيل **Dependabot** أو **Snyk** لمراقبة الثغرات في مكتبات npm و pip وتحديثها تلقائياً.

---

## 6. خطة التنفيذ (Implementation Timeline)

### المرحلة 1: الحماية الأساسية (أسبوع 1-2) 🔴 "عاجل"
- [ ] تركيب NextAuth.js وتفعيل تسجيل الدخول بجوجل.
- [ ] إعداد قاعدة بيانات Postgres بدلاً من SQLite/Memory.
- [ ] تطبيق Rate Limiting (Redis-based) على جميع الـ API Endpoints.
- [ ] تفعيل GitHub Dependabot.

### المرحلة 2: البنية التحتية الآمنة (أسبوع 3-4) 🟡 "متوسط"
- [ ] نقل الـ Python Agent إلى حاوية Docker معزولة (Sandbox).
- [ ] إعداد سجلات المراقبة (Logging Setup).
- [ ] كتابة اختبارات أمنية (Unit Tests for Security Constraints).

### المرحلة 3: الامتثال والتوسع (شهر 2+) 🟢 "متقدم"
- [ ] إجراء اختبار اختراق (Penetration Test) خارجي.
- [ ] إعداد وثائق الامتثال (GDPR Privacy Policy).
- [ ] تطبيق CI/CD Security Pipeline كامل.

---

## 7. الأدوات الموصى بها (Recommended Toolkit)

| الفئة | الأداة المقترحة (Open Source / Free Tier) | الأداة المقترحة (Enterprise) |
|-------|-------------------------------------------|------------------------------|
| **Auth** | NextAuth.js + Google Auth | Auth0 / Okta |
| **Database** | Supabase (Postgres) | AWS RDS / PlanetScale |
| **Secrets** | GitHub Secrets / .env | AWS Secrets Manager / Vault |
| **Scanner** | OWASP ZAP / Trivy | Burp Suite Professional |
| **Monitoring** | Prometheus + Grafana | Datadog / New Relic |
| **Sandboxing** | Docker (Restricted) | E2B / Firecracker MicroVMs |

---

**تم إعداد هذا التقرير بتاريخ 24 يناير 2026 بواسطة: AI-Architect Security Agent**
