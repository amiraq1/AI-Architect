# 👥 نظام إدارة المستخدمين المتقدم (User Management System)

**المشروع:** نبض (Nabd SaaS)
**الهدف:** تمكين المشرفين من السيطرة الكاملة على صلاحيات المستخدمين، الفرق، والاشتراكات.

---

## 1. المعمارية الهرمية (Hierarchical Architecture)

سنعتمد نموذج **RBAC (Role-Based Access Control)** لتنظيم الصلاحيات:

### الأدوار (Roles) والصلاحيات (Permissions)

| الدور | الصلاحيات |
|-------|-----------|
| **Super Admin** | وصول كامل لكل شيء (إدارة الفواتير، حذف المستخدمين، كشف الـ Logs). |
| **Support Agent** | قراءة محادثات المستخدمين (لحل المشاكل)، تعديل الاشتراكات، لا يمكنه الحذف. |
| **Content Editor** | تعديل النصوص، تحسين الـ Prompts، لا يمكنه رؤية بيانات المستخدمين. |
| **User (Enterprise)** | مدير الفريق في الشركة، يمكنه إضافة أعضاء لفريقه فقط. |

---

## 2. مصفوفة الميزات (Feature Matrix)

### أ. إدارة المستخدمين (CRUD)
*   [x] **قائمة ذكية:** تدعم الـ Infinite Scroll والبحث الفوري (Server-Side).
*   [ ] **الملف الشخصي 360:** عرض كل تفاصيل المستخدم في مكان واحد (المدفوعات، المحادثات، التنبيهات).
*   [ ] **التحكم في الجلسات:** زر "Force Logout" لإنهاء الجلسات المشبوهة.

### ب. الفرق والشركات (B2B Features)
*   **Team Workspace:** الشركات يمكنها إنشاء مساحة عمل ودعوة موظفين.
*   **Shared Credits:** رصيد الرموز (Tokens) يكون مشتركاً لكل الفريق، ويتحكم المدير بالحدود (Limits) لكل موظف.

### ج. سجل النشاط (Activity Log)
*   تسجيل كل حركة حساسة: `User X changed User Y role to Admin`.
*   تخزين الـ Logs لمدة 6 أشهر لأغراض الامتثال القانوني.

---

## 3. تجربة المستخدم (Admin UX)

*   **الأمان أولاً:** عند محاولة حذف مستخدم أو ترقية صلاحياته، يجب طلب تأكيد إضافي (أو 2FA).
*   **الإجراءات الجماعية (Bulk Actions):** تحديد 50 مستخدم وتفعيل اشتراكهم بضغطة واحدة.

---

## 4. نموذج البيانات المقترح (Data Schema)

تحديث `schema.prisma` لدعم الفرق والأدوار:

```prisma
model User {
  id        String   @id @default(cuid())
  role      Role     @default(USER)
  teamId    String?
  logs      ActivityLog[]
}

model Team {
  id        String   @id @default(cuid())
  name      String
  plan      Plan     @default(FREE)
  members   User[]
  tokenLimit BigInt
}

enum Role {
  SUPER_ADMIN
  SUPPORT
  USER
}
```

---

## 5. خطة التنفيذ (Implementation Steps)

1.  **الصفحة الحالية (`/admin/users`)**: هي الأساس، سنقوم بربطها بـ API حقيقي.
2.  **إضافة زر "تعديل"**: يفتح نافذة منبثقة (Modal) لتغيير الصلاحيات.
3.  **بناء نظام الـ Logs**: أي تعديل يتم عبر الـ Admin Panel يجب أن يُسجل في جدول `ActivityLog`.

هذا النظام يضمن أن "نبض" جاهز للعمل مع الشركات الكبيرة (Enterprises) التي تتطلب فصل الصلاحيات والتحكم الدقيق.
