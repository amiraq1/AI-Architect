# 🚀 دليل النشر (Deployment Guide) - Nabd AI

مبروك! لقد وصلت إلى مرحلة إطلاق مشروع "نبض" للعالم. إليك الخطوات الدقيقة لنشر التطبيق على Vercel وضمان عمله بكفاءة.

---

## 1. التجهيزات الأولية (Prerequisites)

تأكد من أن لديك الحسابات التالية:
*   [Vercel Account](https://vercel.com) (للاستضافة)
*   [Groq Cloud API](https://console.groq.com) (للذكاء الاصطناعي)
*   [Auth.js Secret](https://generate-secret.vercel.app/32) (للمصادقة)

---

## 2. إعداد المتغيرات (Environment Variables)

عند إنشاء المشروع الجديد على Vercel، أضف المتغيرات التالية:

| Variable | Value | Description |
|---|---|---|
| `GROQ_API_KEY` | `gsk_...` | مفتاح Groq الخاص بك |
| `AUTH_SECRET` | `(generated_secret)` | مفتاح تشفير الجلسات |
| `DATABASE_URL` | `postgresql://...` | (اختياري) حالياً نستخدم SQLite محلياً، للإنتاج استخدم Vercel Postgres |
| `NEXT_PUBLIC_APP_URL` | `https://nabd.amiraq.online` | رابط الموقع النهائي |

---

## 3. خطوات الرفع على Vercel

1.  تأكد من رفع الكود إلى **GitHub**.
2.  اذهب إلى لوحة تحكم Vercel واضغط **Add New Project**.
3.  اختر مستودع `nabd-saas`.
4.  في إعدادات الـ **Build & Development Settings**:
    *   Framework Preset: `Next.js`
    *   Command: `npm run build`
5.  أضف المتغيرات البيئية (Environment Variables) المذكورة أعلاه.
6.  اضغط **Deploy**.

---

## 4. إعداد الدومين (Domain Setup)

1.  في Vercel، اذهب إلى: `Settings` > `Domains`.
2.  أضف النطاق الخاص بك: `nabd.amiraq.online`.
3.  قم بتحديث سجلات DNS لدى موفر الدومين (Namecheap/GoDaddy) كما تطلب Vercel (عادة `CNAME` إلى `cname.vercel-dns.com`).

---

## 5. ملاحظات مهمة للإنتاج (Production Notes)

### قاعدة البيانات (Database)
المشروع حالياً مهيأ ليعمل بقاعدة بيانات SQLite (`dev.db`) لسهولة التطوير. عند النشر الفعلي، يجب الانتقال إلى PostgreSQL:
1.  أنشئ قاعدة بيانات على Vercel Postgres أو Supabase.
2.  حدث `schema.prisma` من `provider = "sqlite"` إلى `provider = "postgresql"`.
3.  حدث متغير `DATABASE_URL`.
4.  شغل الأمر: `npx prisma db push`.

### التخزين (Storage)
نظام رفع الملفات حالياً يستخدم "Mock Storage" (تخزين وهمي). يجب تفعيل Cloudflare R2 أو AWS S3 كما هو مشروح في `FILE_UPLOAD_PLAN.md`.

---

## 6. الدعم الفني

إذا واجهت أي مشكلة (error 500)، توجه إلى تبويب `Logs` في Vercel لمشاهدة تفاصيل الخطأ.

🚀 **Go launch and make Iraq proud!**
