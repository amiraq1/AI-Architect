# 🎛️ خطة تطوير لوحة التحكم الإدارية (Admin Dashboard Roadmap)

**المشروع:** نبض (Nabd)
**الحالة الحالية:** واجهة مستخدم (UI Shell) ثابتة.
**الهدف:** تحويلها إلى مركز قيادة وتحكم متكامل يعتمد على البيانات الحية.

---

## 1. تحليل الوضع الحالي (Current State Analysis)

### ✅ نقاط القوة (Strengths)
*   **التصميم البصري:** واجهة "Deep Nebula" عصرية جداً، تستخدم Glassmorphism بشكل احترافي، مما يجعل تجربة الإدارة مريحة للعين (Dark Mode).
*   **الأساس المتجاوب:** التخطيط يعمل بشكل جيد على الشاشات الكبيرة، مع هيكلية (Sidebar + Main Content) قياسية.
*   **الدعم العربي:** الواجهة مصممة خصيصاً لاتجاه RTL (اليمين لليسار) والخطوط العربية.

### ⚠️ نقاط الضعف والفرص (Weaknesses & Opportunities)
*   **هيكلية الصفحة الواحدة:** الشريط الجانبي (Sidebar) مدمج داخل `page.tsx`. يجب نقله إلى `layout.tsx` لضمان استمراريته عند التنقل بين الصفحات الفرعية.
*   **البيانات الثابتة:** جميع الأرقام والجداول هي بيانات وهمية (Mock Data). لا يوجد اتصال بقاعدة البيانات.
*   **نقص التفاعل:** لا يمكن فرز الجداول، أو فلترتها، أو البحث فيها.
*   **غياب الرسوم البيانية الحقيقية:** الرسم البياني الحالي هو مجرد مجسم (Mockup) باستخدام CSS.

---

## 2. الهيكلية المعمارية المقترحة (Proposed Architecture)

لتحويل اللوحة إلى نظام متكامل، سنقوم بإعادة هيكلة المجلدات كالتالي:

```
src/app/admin/
├── layout.tsx        # (جديد) يحتوي على Sidebar + Header المشترك
├── page.tsx          # نظرة عامة (Overview Dashboard)
├── users/            # إدارة المستخدمين
│   ├── page.tsx      # جدول المستخدمين
│   └── [id]/         # ملف المستخدم وتعديل الصلاحيات
├── analytics/        # التحليلات المتقدمة
│   └── page.tsx
├── content/          # إدارة المحتوى (CMS)
│   └── page.tsx
└── settings/         # إعدادات النظام
    └── page.tsx
```

---

## 3. خطة التطوير الشاملة (Detailed Development Plan)

### المرحلة 1: التأسيس والبنية (Foundation) 🏗️
1.  **فصل التخطيط (Layout Extraction):**
    *   نقل كود `Sidebar` و `Header` إلى ملف `src/app/admin/layout.tsx`.
    *   ضمان أن حالة "القائمة النشطة" تتحدث تلقائياً بناءً على المسار الحالي (`usePathname`).

2.  **مكونات البيانات (Data Components):**
    *   إنشاء مكون `DataTable` قابل لإعادة الاستخدام يدعم:
        *   Pagination (التصفح).
        *   Sorting (الترتيب).
        *   Search (البحث الفوري).
    *   استخدام مكتبة **TanStack Table** لإدارة منطق الجداول المعقد.

### المرحلة 2: البيانات الحية (Real Data Integration) 💾
1.  **لوحة القيادة (Dashboard):**
    *   ربط بطاقات الإحصائيات (Stats Cards) بـ API حقيقي (`/api/admin/stats`) يجلب البيانات من قاعدة البيانات.
    *   استخدام **Recharts** لعرض رسم بياني حقيقي لنشاط المستخدمين بدلاً من الـ DIVs الحالية.

2.  **إدارة المستخدمين (User Management):**
    *   عرض قائمة المستخدمين المسجلين فعلياً.
    *   إضافة أزرار إجراءات: (حظر مستخدم، ترقية خطة، إعادة تعيين كلمة مرور).

### المرحلة 3: التحليلات المتقدمة (Analytics & Reporting) 📊
1.  **تحليل المحادثات:**
    *   مراقبة عدد الرموز (Tokens) المستهلكة لكل مستخدم.
    *   تحليل أكثر "الوكلاء" (Agents) استخداماً (المبرمج vs الكاتب).
2.  **التصدير:**
    *   إضافة زر "تصدير CSV" لجميع الجداول.
    *   إنشاء تقارير PDF تلقائية للإدارة الشهرية.

### المرحلة 4: الأمان والمراقبة (Security & Monitoring) 🛡️
1.  **سجلات التدقيق (Audit Logs):**
    *   تتبع كل إجراء يقوم به المسؤول (من قام بتعديل ماذا ومتى؟).
2.  **الصلاحيات (RBAC):**
    *   إنشاء نظام أدوار دقيق:
        *   `Super Admin`: وصول كامل.
        *   `Support`: وصول للقراءة وحل المشاكل فقط.
        *   `Editor`: إدارة المحتوى فقط.

---

## 4. تصميم تجربة المستخدم (Admin UX Improvements)

### 🎨 نظام الألوان والأسلوب
سنحافظ على هوية "Deep Nebula" ولكن مع تحسينات لزيادة "كفاءة المعلومات":
*   **تقليل الضجيج البصري:** تقليل التوهج (Glow) في الجداول لزيادة وضوح البيانات.
*   **التباين العالي:** استخدام ألوان مميزة للحالات (أخضر=نشط، أحمر=محظور، أصفر=معلق).

### ⚡ الأداء
*   **SWR / TanStack Query:** لاستخدام استراتيجية `stale-while-revalidate`. البيانات تظهر فوراً (من الكاش) ويتم تحديثها في الخلفية.
*   **Optimistic UI:** عند تعديل مستخدم، تتحدث الواجهة فوراً قبل انتظار استجابة السيرفر.

---

## 5. توصيات التنفيذ الفوري (Action Items)

نوصي بالبدء فوراً في **المرحلة 1**:
1.  إنشاء `src/app/admin/layout.tsx`.
2.  تثبيت المكتبات الضرورية:
    ```bash
    npm install recharts @tanstack/react-table clsx tailwind-merge
    ```
3.  بناء مكون `StatsCard` ومكون `LineChart` ليكونوا قابلين لإعادة الاستخدام.

---

هذه الخطة تضمن تحويل لوحة التحكم من مجرد "واجهة جميلة" إلى "دماغ مركزي" يدير كامل عمليات منظومة نبض بكفاءة عالية.
