# 🌐 خطة التصفح والبحث (Web Browsing Plan)

**المشروع:** نبض (Nabd SaaS)
**الحالة:** تم بناء الـ Backend (API)، بانتظار تفعيل الـ Frontend Integration.

---

## 1. كيف يعمل النظام؟

1.  **المستخدم:** يطلب "ابحث لي عن سعر الدولار اليوم".
2.  **الذكاء الاصطناعي (LLM):** يدرك أنه لا يملك المعلومة، فيرد بـ "Tool Call" خاص (يتم تنفيذه لاحقاً في الـ Frontend).
3.  **النظام:** يستخدم `api/browse` لجلب محتوى الصفحة المطلوبة.
4.  **الرد النهائي:** يقوم الذكاء الاصطناعي بتلخيص المحتوى الذي تم جلبه.

## 2. أمان المتصفح (Browser Security)

لحماية السيرفر من هجمات SSRF (Server-Side Request Forgery)، تم تطبيق القيود التالية في `src/app/api/browse/route.ts`:

*   ❌ منع الوصول لـ `localhost` أو `127.0.0.1`.
*   ❌ منع البروتوكولات غير `http/https` (مثل `file://`).
*   ⏱️ مهلة قصوى (Timeout) قدرها 8 ثوانٍ.
*   ✂️ قص المحتوى الزائد (Max 15,000 chars) لتوفير التوكناز.

## 3. كيفية الاختبار (Testing)

يمكنك اختبار نقطة النهاية (Endpoint) باستخدام `curl` أو Postman:

```bash
POST /api/browse
Content-Type: application/json

{
  "url": "https://example.com"
}
```

## 4. الخطوات التالية (Future Work)

1.  إضافة دعم **محثات البحث (Search Queries)**: استخدم API مثل Google Custom Search للحصول على قائمة روابط، ثم استخدم `browse` لقراءة محتواها.
2.  تحسين `extract-content.ts` لدعم مكتبات أقوى مثل `JSDOM` إذا انتقلنا من Edge Runtime إلى Node.js Runtime.

---
هذا الأساس يمنح "نبض" عيوناً على العالم الخارجي! 👀
